---
"opencode-supabase": patch
---

Pin the published dependency contract (exact `@opencode-ai/plugin` and `open` versions, drop unused `@opencode-ai/sdk` and `zod`) and make the packed TUI build rewrite host-runtime imports in an import-aware way.
